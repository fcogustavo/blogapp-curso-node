<h3>Categoria:</h3>
<div class="card">
    <div class="card-body">
        <form action="/admin/categories/edit" method="post">
            <label for="name">Nome: </label>
                <input type="text" name="name" id="name" placeholder="Nome da categoria" value="{{category.name}}" class="form-control">
            <label for="slug">Slug: </label>
                <input type="text" name="slug" id="slug" placeholder="Slug da categoria" value="{{category.slug}}" class="form-control">
            <input type="hidden" name="id" id="id" value="{{category._id}}"><br>
            <button type="submit" class="btn btn-success">Editar categoria</button>
        </form>
    </div>
</div>