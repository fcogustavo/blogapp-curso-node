<div class="card">
    <div class="card-body">
        <h1>{{post.title}}</h1>
        <hr>
        <small>Data de Publicação: {{post.date}}</small>
        <hr>
        <div>
            {{post.content}}
        </div>
    </div>
</div>